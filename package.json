{
  "name": "frontend",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "intersection-observer": "^0.12.2",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-is-visible": "^1.2.0",
    "react-scripts": "^5.0.1",
    "react-transition-group": "^4.4.5",
    "use-local-storage": "^2.3.6",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "start": "sudo PATH=/opt/node/bin:$PATH NODE_OPTIONS=--max_old_space_size=1536 PORT=80 ./node_modules/.bin/react-scripts start",
    "build": "sudo PATH=/opt/node/bin:$PATH NODE_OPTIONS=--max_old_space_size=1536 ./node_modules/.bin/react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
